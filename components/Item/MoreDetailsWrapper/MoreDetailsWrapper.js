import { Stack, Text } from "@chakra-ui/react";

export const MoreDetailsWrapper = ({ description = "" }) => {
  return (
    <Stack direction="row" alignItems="center" justifyContent={"center"}>
      <Text></Text>
    </Stack>
  );
};
